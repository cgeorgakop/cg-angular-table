import { SortTableColumnDirective } from './sort-table-column.directive';

describe('SortTableDirective', () => {
  it('should create an instance', () => {
    const directive = new SortTableColumnDirective();
    expect(directive).toBeTruthy();
  });
});
